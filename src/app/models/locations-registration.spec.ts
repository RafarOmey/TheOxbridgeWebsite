import { LocationsRegistration } from './locations-registration';

describe('LocationsRegistration', () => {
  it('should create an instance', () => {
    expect(new LocationsRegistration()).toBeTruthy();
  });
});
